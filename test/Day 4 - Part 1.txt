Notul Day 4

Pakai VSCode, ada terminal

git essentials

Zunan, DECRYPT, 13518019 mamet

enggak lebih jago dari kalian, kalau ada salah dikoreksi aja, ada yang lebih jago

git salah satu version controlling.
laporan 1, 2, 3, 4 fix banget bismillah. Bikin kode kayak gitu juga enggak mungkin. Makanya pake git.

Google Docs juga ada version historynya, yang kontribusi siapa aja. git itu tools buat developer. Kenapa git? branching, pengembangan tersebar, dan kolaborasi.

collaboration platform untuk git: github, gitlab, bitbucket. Di lokal ada, di google drive ada, sama kayak git dan github. Git bisa jalanin sendiri di lokal kita untuk ngetrack versioning di laptop kita, github kayak google drive, repository remote. Github itu kayak cloud buat kolaborasi.

Ada 3 orang mau kolaborasi. Ngetrack masing2 di laptop mereka masing2. Punya remote repository kayak folder gitu, kolaborasi ke satu remote yang sama. Kalau di google drive bisa sinkron antara lokal dan remote, bisa sync. Kalau ini, git punya command2 sendiri, push, pull, commit, merge.

git bash, kalau di windows, git bash, di linux/ubuntu, tinggal install ??. Git bash, unix style command line experience. Bedain git dan github, git toolsnya, github remote repositorynya yang mengimplementasikan peraturan di git. git juga nyediain gui buat push, tapi cuma di Windows sama Mac. Developer juga harus biasakan git bash command line. Entar biasanya waktu deploy aplikasi ke cloud, interface cloud cuma terminal doang, harus biasain command line buat git atau apapun.

Kalau lupa command, bisa $ git --help
balik dari git --help -a: ctrl + z aja

git bash itu berdasar terminal, kita ngepush, clone, add di terminal, lewat command line. Misal tadi help, keluar keterangan singkat. GUI lebih mudah, ada interface, ngelakuin hal yang sama. Dulu pake GUI juga. Makin kesini makin ...

command2 di gitbash:
init: membuat repository baru. init di terminal, atau bikin repo di github. $ git init

clone: duplikasi repository ke local. repository harus bersifat publik? bisa login dulu sih

add: menambahkan file ke dalam, agar perubahan file tersebut tercatat saat commit.

commit: checkpoint terhadap perubahan
push
pull

Karena ada perubahan di remoter repository yang tidak tercatat di lokal, sehingga remote tidak bisa diupdate dari lokal. Bila file yang sama memiliki perubahan yang berbeda, maka terjadi conflict.

branch, merge, fetch, log, blame
fetch; mengambil data terbaru dari repository. bedanya sama pull, fetch enggak di-merge.
log: melihat status


etika git
flag -f itu force. Misal kita push pake -f, konflik enggak terjadi, di tempat kita ada 10 versi misalnya. Kita di lokal 10 versi, git add commit -f. Di remote harus disamain sama punya kita.

Sebelum bekerja, cek repository berubah atau tidak

Comment bermakna: add navbar and update style.css, authentication error fixed, fixing push button in page home didn't work
tidak bermakna: masih ada bug, sudah fix plis, bismillah fix v2, aduh nyerah

solve conflict ada gui dari vscodenya, kalau notepad ada tambahan line 6 sampai 10. Gitnya bikin line yang misahin, bedanya itu ini. Sebenarnya bisa hapus manual.
Baru pake git add .
git commit -m "solve conflict".

Bedanya di sublime atau yang lain, harusnya ada juga install plugin gitu. Delete headnya tadi, pingin perubahan yang mana pilih.

commitnya pake message, nim dan nama.

Login ke VSCode pake akun github





